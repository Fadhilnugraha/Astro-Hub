using UnityEngine;
using UnityEngine.SceneManagement;

public class artikel2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void btnback()
    {
        SceneManager.LoadScene("Main menu");
    }
}
